let ventana = window.innerHeight;

let documento = document.body.offsetHeight;


// la altura de la ventana + el punto Y de la pagina es mayor a la altura del documento + el margen para que no llegue al final del todo antes de cargar mas  
endOfPage = ventana + window.pageYOffset >= documento + (margen);



var peticion = false

if (!peticion) {

}else {

}

//peticion en curso metida en el fetch y cuando termine cambiar la variable

//dentro de ese tiempo de espera meter el gif de carga;